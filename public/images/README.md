Taruh foto di sini (hero, mempelai, galeri, cerita, dll), lalu isi field `image` di
`data/content.json` dengan path `/images/nama-file.jpg`.

Contoh: file `public/images/hero.jpg` dipakai di `data/content.json` sebagai:

```json
"hero": { "image": "/images/hero.jpg" }
```
